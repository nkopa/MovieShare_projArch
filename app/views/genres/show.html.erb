<% provide(:title, @genre.name) %>

<div class="panel panel-default">
  <div class="panel-heading">Nazwa</div>
  <div class="panel-body">
    <%= @genre.name %>
  </div>
</div>
<!--p>
  <strong>Name:</strong>
  <%= @genre.name %>
</p-->

<%= link_to 'Edytuj', edit_genre_path(@genre) %> |
<%= link_to 'Wróć', genres_path %>
